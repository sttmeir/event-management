H2 database can be accessed through: http://localhost:8081/h2-console

Controller methods can be checked through Postman or Talend API google extension. 

After the start up of the application, the default user with "parser" username and "qwerty" password are created. Use these credentials
for auth. 

